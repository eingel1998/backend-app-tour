# ===== CONFIGURACIÓN DE ENTORNO =====
# Copia este archivo como .env.local y completa con valores para tu entorno.
# No subas .env.local a Git si contiene secretos reales.

# ===== DESARROLLO LOCAL OBLIGATORIAS =====
NODE_ENV=development
# Genera una clave secreta segura de 32 caracteres para Payload CMS.
# Puedes usar `openssl rand -base64 32` o un generador online.
PAYLOAD_SECRET=your-32-character-secret-key-here

# --- Configuración de Base de Datos ---
# URI de conexión a la base de datos. Para SQLite local, usa 'file:./nombre-de-tu-db.db'.
# Para PostgreSQL o MongoDB, usa el formato de URI correspondiente.
DATABASE_URI=file:./backend-app.db

# Token de autenticación para la base de datos (ej. para Turso en producción).
# Para SQLite local, generalmente se deja vacío.
DATABASE_AUTH_TOKEN=

# Directorio donde se almacenarán los backups de la base de datos.
DATABASE_BACKUP_DIR=./backups

# Número máximo de conexiones permitidas a la base de datos (si aplica al adaptador).
# Para SQLite, esto es menos relevante ya que las conexiones son al archivo.
DATABASE_MAX_CONNECTIONS=10

# Tiempo de espera (timeout) para las operaciones de base de datos en milisegundos (si aplica al adaptador).
DATABASE_TIMEOUT=30000


# ===== CONFIGURACIÓN OPCIONAL DE SERVIDOR =====
# Puerto en el que correrá la aplicación Payload.
# PORT=3000

# Orígenes CORS permitidos (separados por coma).
# CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# URL pública completa de tu servidor Payload (usada para construir URLs absolutas).
# NEXT_PUBLIC_SERVER_URL=http://localhost:3000


# ===== PARA PRODUCCIÓN (EJEMPLO RENDER) =====
# NODE_ENV=production
# PAYLOAD_SECRET= # ¡DEBE SER UN SECRETO REAL GENERADO PARA PRODUCCIÓN!
# DATABASE_URI= # URI de tu base de datos de producción (ej. Turso, Neon, MongoDB Atlas)
# DATABASE_AUTH_TOKEN= # Si tu DB de producción usa un token (ej. Turso)
# NEXT_PUBLIC_SERVER_URL=https://tu-app-name.onrender.com # URL pública de tu app en Render
# PORT=10000 # Render usa este puerto internamente


# ===== INTEGRACIONES OPCIONALES (EJEMPLOS) =====
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=

# ===== NOTAS IMPORTANTES =====
# 1. El archivo `.env.local` (copia de este) NUNCA debe subirse a Git si contiene secretos.
#    Asegúrate de que `.env.local` esté en tu `.gitignore`.
# 2. Genera un `PAYLOAD_SECRET` único y seguro para cada entorno, especialmente producción.
# 3. Para entornos de producción (como Render, Vercel, etc.), configura estas variables
#    directamente en el panel de control de tu proveedor de hosting.